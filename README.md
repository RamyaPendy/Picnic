# Picnic
gist api automation using python

Requires python 3

Install all dependencies: 
$pip install requests
$pip install pandas

Before running the test, please enter credentials in config.py file

Run makefile
